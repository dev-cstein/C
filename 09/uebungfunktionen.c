#include <stdio.h>
#include <stdbool.h>

float eingabeNote(int note); 
int main(void)
{
	sizeof_array = sizeof(note) / sizeof(note[0]); 
	int note[10];
	bool auswahl;
}

float eingabeNote(int note);
{	
	while(true)
	{
		printf("Bitte Note eingeben: ");
		scanf("%f", &note);

		if(sizeof_array < 10)
		{
			printf("Weitere Note eingeben? 1 = JA, 0 = NEIN");
			scanf("%i",  
		

	}
}
